// constants.js

// Object containing paths to various directories used in the project.
// Currently, it includes the path to the 'videos' directory.
const PATHS = {
    VIDEOS: './videos/', // Path to the videos directory
};

// The port number on which the server will listen.
// This is used to configure the network port for server operations.
const PORT = 3000;

// Exporting PATHS and PORT for accessibility in other parts of the project.
module.exports = { PATHS, PORT };
